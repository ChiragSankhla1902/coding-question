#include<bits/stdc++.h>
using namespace std;

int main(){
    int i,size,temp;
    cin>>size;
    vector <int> arr;
    for(i=0;i<size;i++){
        cin>>temp;
        arr.push_back(temp);
    }
    sort(arr.begin(),arr.end());
    for(i=0;i<size;i++){
        cout<<arr[i]<<" ";
    }
}

